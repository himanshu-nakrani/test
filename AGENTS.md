## Cursor Cloud specific instructions

This repository is currently empty (only contains `.gitattributes`). There is no application code, build system, package manager, test suite, or lint configuration.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, or similar.
- **No services to run** — no backend, frontend, or database.
- **No lint/test/build commands** — none are configured.

Once code is added to this repository, this section should be updated with:
1. How to install dependencies
2. How to run lint, tests, and the dev server
3. Any non-obvious caveats discovered during setup
